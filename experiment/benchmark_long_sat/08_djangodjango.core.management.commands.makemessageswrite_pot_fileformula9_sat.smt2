(set-logic ALL)
(declare-const potfile String)
(declare-const msgs String)
(assert (not (< (str.len msgs) 0)))
(assert (= (+ 0 (str.indexof msgs "\u{a}" 0)) (- 1)))
(assert (str.contains msgs "charset=CHARSET"))
(check-sat)
(get-value (potfile))
(get-value (msgs))
